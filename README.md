 ## Run dependencies installation script before running notebooks
```
chmod +x src/install_packages.sh
./src/install_packages.sh
```

## Notebook execution order
1. StaticData
2. DynamicData
3. ForwardSimulator
